<h1><%= @firstmodel.name %> Page</h1>

<ul>
  <% @firstmodel.secondmodels.each do |secondmodel| %>
  <li><%= secondmodel.name %> | <%= link_to "View secondmodel", firstmodel_secondmodel_path(@firstmodel, secondmodel) %></li>
  <% end %>
</ul>

<%= link_to "Edit", edit_firstmodel_path(@firstmodel) %> |

<%= link_to "Delete", firstmodel_path(@firstmodel), :data => {:confirm => "You Sure?", :method => 'delete'} %> |

<%= link_to "Add a secondmodel", new_firstmodel_secondmodel_path(@firstmodel) %>

<hr>

<%= link_to "Back to Homepage", firstmodels_path %>
